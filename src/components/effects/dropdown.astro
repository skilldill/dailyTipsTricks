---
import EffectCard from '../EffectCard.astro';

const title = 'Dropdown';
const slug = 'dropdown';
const description = 'Эффект: Dropdown.';
const label = 'with interactive';
const techs = ['HTML', 'CSS', 'JS'];
---

<EffectCard title={title} slug={slug} description={description} label={label} techs={techs} />
