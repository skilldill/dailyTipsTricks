---
import EffectCard from '../EffectCard.astro';

const title = 'Sunrise Effect';
const slug = 'sunriseEffect';
const description = 'Эффект: Sunrise Effect.';
const label = 'after loading';
const techs = ['HTML', 'CSS'];
---

<EffectCard title={title} slug={slug} description={description} label={label} techs={techs} />
